Refs #249 — remaining slices: E2 trip geo read, slice 3 map canvas, slice 4 rich facets, slice 5 global trip map.

- E2 (`ebce189`): `GET /api/trips/geo` (authenticated, same actor resolution as `GET /api/trips`) returns one row per LISTABLE trip — the `discoverable OR viewer-has-role` rule from `_profile_trips`, re-checked in Python — each with `dtId, title, stage` + anchor `{lat, lng, name}` (first located registry entry; unlocated trips omitted, never 0,0). Cards, not documents; 60 s `@_cached_graph` + `_CREW_CACHED_READS`; graph disabled → `[]`. Key files: `backend/app/graph/client.py` (`_Q_GEO_MINE/_Q_GEO_DISCOVERABLE`, `list_geo_trips`, `_geo_row_from_dict`), `backend/app/store.py` (`list_geo_trips` + anon fallback), `backend/app/main.py` (`GET /api/trips/geo` before `/{trip_id}`). Tests: `backend/tests/test_geo_249.py` (26: query text, mapper allowlist/omission/order, merge dedupe + stranger refusal, fallback, endpoint 401/shape/collapse).
- Slice 3 (`46a9a29`): signed-in home on the §2.2 canvas — `SplitView` + `Sheet` ladder (fixed left rail ≥1280px, full-bleed map + three-detent sheet on phones), bands moved as-is into the rail/sheet furniture, pins from E2 via `components/HomeMap.tsx` (LandingMap pattern: keyless positron, `pinClassForStage`, `route-pin/is-selected/route-map-focused` grammar, count-badge clustering, real-button pins + zoom controls, placeholder-first SSR). Band↔pin linkage is the #104 chip↔card idiom (row hover/focus raises pin; pin tap scrolls + `place-pill-flash`es the row). Map collapses with bands as-is on empty/failed geo. Key files: `frontend/src/lib/types.ts` (`TripGeo`), `lib/api.ts` (`fetchTripGeo`, never throws), `lib/home-geo.ts` (pure pins/stage/cluster/row-id), `components/HomeMap.tsx`, `pages/LandingPage.tsx`. Tests: `lib/home-geo.test.ts`, `components/HomeMap.test.tsx` (mocked loader: one marker per pin, stage classes, selection, clustering, empty/no-WebGL2), `pages/LandingPage.test.tsx` (+SplitView stub, geo stub, canvas/collapse/linkage).
- Slice 4 (`64db597`): rich facets in `lib/home.ts` (`TripFilter` + `monthOfTrip` + `SEASON_MONTHS` + `TripOrigin`, order-preserving sort-then-filter contract kept) + chips UI above the bands (place/region input, season chips, All/Mine/Following provenance, public/private). Feed rows keep the text-only rule (extended to place text). Tests: `lib/home.test.ts` facet matrix + page-level facet tests.
- Slice 5 (`83416a9`): discoverable layer + pin cards on the SAME canvas — floating toggle (default ON, only when both layers non-empty), `components/TripPinCard.tsx` (band card language condensed: cover, StageBadge, anchor, one Open link), selection drives pin + row + card together. No new route, `is_spa_route` untouched. Tests: `TripPinCard.test.tsx`, HomeMap zoom tests, page-level toggle/card tests.
- Docs: `DESIGN.md` §2.2 (home built on the map surface), §7.5 IA pointer, §15 item 10 CLOSED with the #249 note.

Tests: backend `uv run pytest` 728 passed, 12 skipped; frontend `pnpm test` 741 passed (64 files); `pnpm build` clean (tsc + vite). Models/DTDL untouched (`gen_dtdl --check` N/A).

Do NOT merge — Niko reviews and merges.
