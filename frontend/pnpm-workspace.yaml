allowBuilds:
  browser-tabs-lock: false
  # core-js's postinstall only prints a funding banner — no build output is
  # needed. (posthog-js pulls it in for its polyfills; issue #21.)
  core-js: false
  esbuild: true
